# AGENTS.md — Nuotao Outdoor AI OS 项目规范

> 本文件是项目内所有 AI Agent 与人类开发者的统一行为准则。
> 适用于本仓库全部目录树；更深层级目录若有冲突，以更具体的 AGENTS.md 为准。
> 规划文档：`docs/project_architecture.md`、`docs/business_context.md`、`docs/development_roadmap.md`

---

## 1. 项目规则

### 1.1 项目定位

- 本项目是 **AI Native Outdoor Commerce Operating System**：DTC 电商 + 五个 AI 角色（产品经理、营销经理、供应链经理、客户经理、商业分析）。
- 业务三阶段：中国直发 DTC → 海外仓 → B2B/代理商。所有设计必须可平滑演进，禁止只满足眼前需求的死胡同方案。

### 1.2 决策铁律

1. **先规划后编码**：任何功能变更前，先更新/补充 `/docs` 规划文档；不写「边写边想」的代码。
2. **最小闭环优先**：先跑通真实业务闭环，再叠加 AI 能力；AI 只做建议，关键业务动作必须有人审（Human-in-the-loop）。
3. **开源优先、低成本**：优先成熟开源方案；付费项（LLM API、支付、物流）必须有明确的成本上限与降级策略。
4. **数据是资产**：从第一天起结构化记录事件与指标，为 AI 与报表提供数据底座。
5. **禁止凭感觉**：选品、定价、营销决策必须基于数据模型，禁止硬编码业务经验值到业务逻辑中（可放入配置/知识库）。

### 1.3 工作流规则

- 分支：功能分支开发 → PR → 评审 → 合并 `main`（单人开发也保留 PR 记录）。
- 提交信息：Conventional Commits：`feat|fix|docs|chore|refactor|test(scope): 描述`。
- 文档同步：改动架构/流程必须同步更新 `/docs` 并升版本号。
- 环境：`dev` / `staging` / `prod` 三套环境；密钥只放 Secrets 管理，绝不提交仓库。
- 备份：数据库每日自动备份到对象存储，定期演练恢复。

### 1.4 技术栈基线（Phase 1）

- 后端：Python 3.12 + FastAPI
- AI 编排：LangGraph + LiteLLM（Gateway，禁止业务代码直连单一模型供应商）
- 数据库：PostgreSQL 16（业务库）+ Redis（缓存/队列）+ pgvector（向量）
- 电商：WooCommerce（自托管，通过 REST/Webhook 集成，禁止直接改其数据库）
- 部署：Docker Compose 起步；CI/CD：GitHub Actions
- 前端控制台：React（Next.js 或 Vite SPA）
- 监控：结构化日志 + Sentry + Prometheus/Grafana

> 新增第三方依赖必须先说明理由、许可证、维护状态；重大选型变更需在 `/docs` 记录决策（ADR）。

---

## 2. 编码规范

### 2.1 通用

- 语言：Python 采用 PEP 8（ruff 校验）；TypeScript 采用 ESLint + Prettier。
- 命名：变量/函数用 `snake_case`（Python）、`camelCase`（TS）；类用 `PascalCase`；常量全大写。
- 类型：Python 全量类型注解（`typing`）；禁止裸 `dict` 传递业务对象，用 Pydantic 模型。
- 注释：解释「为什么」，不写「是什么」；禁止无意义注释与注释掉的代码（删掉即可）。
- 空值：显式处理 `None`/空集合，禁止隐式空值穿透到 AI 提示词或对外文案。

### 2.2 目录结构（约定）

```
app/
  api/            # 路由层：只做参数校验与调用服务层
  services/       # 业务服务层：业务规则唯一来源
  agents/         # AI Agent 定义与工具（只通过 services 访问数据）
  models/         # SQLAlchemy/Pydantic 模型
  schemas/        # API 出入参
  core/           # 配置、安全、日志、依赖注入
  integrations/   # 外部系统适配（WooCommerce/物流/支付/1688）
  tasks/          # 定时任务与后台任务
  migrations/     # Alembic 迁移
tests/
docs/
infra/            # Docker Compose、CI、部署脚本
```

### 2.3 分层约束

- **路由层**不得包含业务逻辑；**服务层**不得直接拼 SQL 之外访问数据库（用 ORM/仓储）。
- **Agent 禁止直连数据库/文件系统**：所有数据访问必须走 `services` 层暴露的白名单 API。
- **集成层**负责外部 API 的鉴权、重试、超时、降级；外部系统错误不得以原始堆栈暴露给前端。
- 业务规则集中在服务层，禁止散落在控制器、Agent 提示词或前端。

### 2.4 错误处理

- 自定义业务异常 + 统一错误码；API 返回结构：`{ "code", "message", "data", "trace_id" }`。
- 所有外部调用必须有超时、重试（幂等接口）、熔断与降级。
- 日志分级：`INFO`（关键动作）、`WARNING`（可恢复异常）、`ERROR`（需告警）；禁止打印密钥与 PII。

### 2.5 测试

- 核心域（订单、履约、成本模型、状态机、Agent 工具）单元测试覆盖率 ≥ 80%。
- 集成测试用 Testcontainers；Agent 评测用固定评测集，禁止用真实用户数据。
- 合并门禁：lint + 单测 + 集成测试 + 依赖漏洞扫描必须全绿。

---

## 3. AI Agent 开发原则

### 3.1 架构原则

1. **Agent 是「提议者」不是「执行者」**：高风险操作（发货、退款、改价、删除）必须进入审批队列，人工确认后执行。
2. **全链路可审计**：每次 Agent 运行的输入、规划、工具调用、输出、成本、审批结果必须落库（`ai_agent_runs`）。
3. **单一模型网关**：所有 LLM 调用走 LiteLLM；按任务类型路由模型，配置配额、缓存与降级。
4. **白名单工具**：Agent 只能调用注册过的工具函数；工具函数内部必须做权限校验与参数校验。
5. **上下文最小化**：只把完成任务所需数据给模型；禁止把整表数据或无关客户 PII 塞入提示词。

### 3.2 提示词与输出规范

- 提示词视为代码：入库管理、版本化、可评审；禁止散落在字符串常量中。
- 所有对外文案（客服回复、营销内容、SEO）必须通过规则校验：禁词、敏感词、品牌口径、事实核对。
- Agent 输出必须结构化（Pydantic 校验），LLM 输出解析失败时走重试/降级，不允许静默吞掉。

### 3.3 人审与降级

- 审批队列：`ai_suggestions` 表中 `待审/通过/拒绝` 状态；任何自动化执行前检查审批状态。
- 降级链：LLM 不可用 → 规则引擎 → 人工；客服场景必须保证「回复不了也比乱回复强」。
- 成本护栏：每 Agent 设月度成本预算，超限自动告警并降级到低配模型。

### 3.4 评测与迭代

- 每个 Agent 必须有评测集（≥ 20 个代表性用例）与评分标准（准确性、合规性、语气）。
- 提示词/模型变更必须跑评测回归，禁止无评测直接上线。
- 线上运行结果抽样人工复核，反馈回流到评测集。

---

## 4. 数据安全原则

### 4.1 数据分级

| 级别 | 范围 | 处理要求 |
|---|---|---|
| L1 公开 | 商品公开信息、营销内容 | 可自由处理 |
| L2 内部 | 成本、供应商、订单统计、经营数据 | 仅限内部，禁止外发 |
| L3 机密 | 供应商合同、定价策略、商业计划 | 加密存储、最小权限 |
| L4 PII | 客户姓名、邮箱、地址、支付信息 | 加密存储、访问留痕、GDPR 合规 |

### 4.2 存储与传输

- 传输：全链路 TLS；密钥用 Secrets 管理，禁止硬编码与提交仓库。
- PII 加密：邮箱/姓名/地址等字段加密存储（如 AES-256 + KMS 或应用层加密）。
- 支付数据：直接由支付服务商（Stripe/PayPal）处理，本地**禁止存储**卡号/CVV 等原始支付凭证。
- 日志/提示词/Agent 运行记录中禁止出现 PII 明文；需要时脱敏（mask/截断）。

### 4.3 访问控制

- 最小权限：DB 账号按应用拆分；管理控制台按角色（管理员/运营/客服/只读）授权。
- 数据导出：支持按客户导出/删除数据（GDPR 要求），删除需留审计日志。
- 第三方接入（物流/1688/LLM）：只传递任务必需字段；对外发送前检查是否含 PII。

### 4.4 合规基线

- 遵循 GDPR 基线：隐私政策、Cookies 同意、数据主体权利流程。
- 遵循 1688/平台数据使用条款；爬取类功能必须先过合规评审。
- 数据泄露应急：事件发现 24h 内上报负责人，72h 内按法规通报。

### 4.5 安全工程

- 依赖：每周漏洞扫描（pip-audit / npm audit），高危漏洞优先修复。
- Web 安全：OWASP Top 10 基线（注入、XSS、CSRF、SSRF、越权）；LLM 输入输出做注入防护。
- 备份：数据库每日备份 + 异地副本；恢复演练每季度一次。

---

## 5. 变更与演进

- 本文档随项目演进迭代；修改需在 PR 中说明理由并同步 `/docs`。
- 新增外部集成、新 Agent、新数据域，必须先补充设计文档再开发。
- 任何「快速临时方案」必须标注 `TODO(tech-debt)` 并登记 Backlog，禁止无限期遗留。
