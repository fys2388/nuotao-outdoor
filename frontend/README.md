# Nuotao AI OS — Frontend（管理控制台占位）

M0 阶段仅初始化目录与基础配置；正式控制台（审批队列、Agent 运行日志、经营看板）将在 M1 按
`docs/project_architecture.md` 规划搭建。技术选型：React + TypeScript + Vite。

```bash
cd frontend
npm install
npm run dev      # 默认 http://localhost:5173
```